name: Validate and Initialize Infrastructure

on:
  push:
    branches:
      - gitactions
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          version: "latest"
          project_id: "idata2502-ci-cd"

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.4.5

      - name: Terraform Validate
        run: terraform validate
        working-directory: infrastructure/iac/terraform/

      - name: Terraform Init
        run: terraform init
        working-directory: infrastructure/iac/terraform/

      - name: Terraform Refresh
        run: terraform refresh
        working-directory: infrastructure/iac/terraform/

      - name: Terraform Plan
        run: terraform plan -var="environment=staging" -out=tfplan
        working-directory: infrastructure/iac/terraform/

      - name: Upload Terraform Plan
        uses: actions/upload-artifact@v3
        with:
          name: terraform-plan
          path: infrastructure/iac/terraform/tfplan

      - name: Terraform Apply with Approval
        if: github.event_name == 'workflow_dispatch'
        run: terraform apply tfplan
        working-directory: infrastructure/iac/terraform/
